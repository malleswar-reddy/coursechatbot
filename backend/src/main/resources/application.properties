# =============================================================================
# CourseChatbot Backend - Application Configuration
# Environment variables override every default shown here.
# =============================================================================

spring.application.name=coursechatbot-backend

# -----------------------------------------------------------------------------
# Startup optimisations
# -----------------------------------------------------------------------------
# Defer bean creation to first use (~40% faster startup)
spring.main.lazy-initialization=true
# Skip ASCII banner
spring.main.banner-mode=off
# Disable JMX (not needed, saves ~100 ms)
spring.jmx.enabled=false
# This enables Virtual Threads for all incoming Web requests
spring.threads.virtual.enabled=true

# -----------------------------------------------------------------------------
# R2DBC ? reactive database access (app queries)
# Override via: SPRING_R2DBC_URL, SPRING_DATASOURCE_USERNAME/PASSWORD
# -----------------------------------------------------------------------------
spring.r2dbc.url=${SPRING_R2DBC_URL:r2dbc:postgresql://localhost:5432/coursechatbot}
spring.r2dbc.username=${SPRING_DATASOURCE_USERNAME:chatbot}
spring.r2dbc.password=${SPRING_DATASOURCE_PASSWORD:chatbot_secret}

# Connection pool ? start small, grow on demand
spring.r2dbc.pool.initial-size=2
spring.r2dbc.pool.max-size=10
spring.r2dbc.pool.max-idle-time=30m

# -----------------------------------------------------------------------------
# JDBC DataSource ? used ONLY by Flyway for schema migrations
# Override via: SPRING_DATASOURCE_URL
# -----------------------------------------------------------------------------
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/coursechatbot}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:chatbot}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:chatbot_secret}
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway only needs 1 connection briefly ? keep Hikari tiny
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.connection-timeout=10000

# -----------------------------------------------------------------------------
# Flyway ? schema migrations
# -----------------------------------------------------------------------------
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# -----------------------------------------------------------------------------
# Ollama LLM
# Override via: OLLAMA_BASE_URL, OLLAMA_MODEL, OLLAMA_TIMEOUT, OLLAMA_NUM_PREDICT
# -----------------------------------------------------------------------------
ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.model=${OLLAMA_MODEL:gemma3:4b}
ollama.embedding-model=${OLLAMA_EMBED_MODEL:nomic-embed-text}
ollama.timeout-seconds=${OLLAMA_TIMEOUT:600}
ollama.num-predict=${OLLAMA_NUM_PREDICT:400}

# -----------------------------------------------------------------------------
# ChromaDB — Vector store for course content
# -----------------------------------------------------------------------------
chroma.base-url=${CHROMA_BASE_URL:http://localhost:8001}
chroma.top-k=${CHROMA_TOP_K:3}

# -----------------------------------------------------------------------------
# Server ? Netty (WebFlux)
# -----------------------------------------------------------------------------
server.port=8080

# -----------------------------------------------------------------------------
# Actuator
# -----------------------------------------------------------------------------
management.endpoints.web.exposure.include=health,info
